# BGP-ROUTING-BASIC
Study about the basic of BGP-Routing in cisco and huawei. The topology is really simple, there are 3 routers. First Router is an ISP wants to ping 8.8.8.8 (looback0 on router A) that has two path, via router B, or directly via router A.
